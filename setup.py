from setuptools import setup

setup(
    name="alber",
    version="1.0",
    author="Peter Chuikov",
    packages=["alber"],
    zip_safe=False,
    # install_requires=[i.strip() for i in open("requirements.txt").readlines()],
)
